{
  "name": "chugoku-football-data",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-results.mjs",
    "check": "node --check app.js && node --check scripts/sync-results.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "playwright": "^1.52.0"
  }
}
